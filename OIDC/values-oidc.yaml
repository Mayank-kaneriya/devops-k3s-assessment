oidc:
  enabled: true
  clientId: "webui-client"
  clientSecret: "YOUR_SECRET"
  issuer: "https://<YOUR_DOMAIN>/auth/realms/hyperplane/.well-known/openid-configuration"
  redirectUri: "https://<YOUR_WEBUI_DOMAIN>/callback"
  scopes:
    - openid
    - profile
    - email

